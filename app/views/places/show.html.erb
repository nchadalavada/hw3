<h1><%= @place.name %></h1>

<h2>Posts</h2>
<ul>
    <% for post in @place.posts %>
      <li>
        <h3> <%= post.title %> <h3>
        <h4> <%= post.description %>, <%= post.posted_on %> <h4>
        </li>
    <% end %>
  </ul>

<a href="/posts/new?place_id=<%= @place.id %>"> Add Post</a>

<a href="/places">Back to all places</a>
